import { AppLayout } from "@/components/AppLayout";
import { ProfileContent } from "@/features/profile/Profile";

export default function Profile() {
  return (
    <AppLayout>
      <ProfileContent />
    </AppLayout>
  );
}
