import { AppLayout } from "@/components/AppLayout";
import { PositionsContent } from "@/features/positions/Positions";

export default function Positions() {
  return (
    <AppLayout>
      <PositionsContent />
    </AppLayout>
  );
}
