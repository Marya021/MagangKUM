import { AppLayout } from "@/components/AppLayout";
import { SupervisorContent } from "@/features/supervisor/Supervisor";

export default function Supervisor() {
  return (
    <AppLayout>
      <SupervisorContent />
    </AppLayout>
  );
}
