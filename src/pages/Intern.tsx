import { AppLayout } from "@/components/AppLayout";
import { InternDataContent } from "@/features/intern/Intern";

export default function Intern() {
  return (
    <AppLayout>
      <InternDataContent />
    </AppLayout>
  );
}
